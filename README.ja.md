# Xo Beta

[English](README.md)

プレイ: https://xo-beta.pages.dev/

**ブラウザネイティブのプライベート直接 P2P 3D バトルロイヤル。最大 4 人の人間を招待し、残りをボットで補充した最大 10 人のルームで最後の 1 人を目指します。**

Xo Beta はデスクトップブラウザ上で完全にクライアントサイド動作します。ルームはフレンド限定で、公開マッチメイキング、専用ゲームサーバー、マルチプレイヤー DB、TURN、課金フォールバックはありません。シグナリングは無課金の公開 Nostr リレー、ゲーム通信は公開 STUN を使う直接 WebRTC で、直接接続に失敗する場合があります。マッチ状態はホストが決定し、ホストは理論上不正が可能です。これはフレンド限定ルームで受け入れるモデルです。表現には、独自のコード生成システムと再配布可能な CC0（パブリックドメイン）のアート・音声パックを組み合わせています。来歴の詳細は [docs/ASSET_MANIFEST.md](docs/ASSET_MANIFEST.md) を参照してください。

---

## 機能

- **完全なマッチ進行** — 輸送機での飛行 → 自由降下 → グライド → 着地 → 物資収集 → ストーム縮小 → 最終サークル → リザルト画面。
- **3 つの手作りアリーナ** — *NEO CITY*（夜のネオン都市）、*OLD FRONT*（大聖堂をランドマークに持つ曇天の歴史的な町）、*EDEN FACILITY*（昼の湖畔研究施設）。
- **高度な移動** — スプリント、しゃがみ、スライディング、ダブルジャンプ、地上・空中ダッシュ、ウォールラン、壁ジャンプ、よじ登り、グラップリングフック、グラウンドパウンド、バニーホップの慣性、水泳、潜水。ボットも同じ移動システムを使います。
- **5 種類の武器クラス** — ピストル（セミオート）、SMG（フルオート）、AR（フルオート）、ショットガン（ポンプアクション）、スナイパー（ボルトアクション）。それぞれに 5 段階のレアリティがあり、飛翔時間、重力落下、距離減衰、跳弾、ショットガンのペレット単位の拡散を含む実弾シミュレーションを行います。
- **公平な AI 対戦相手** — 異なる個性を持つ 9 体のボット（VEX、RAZOR、ORBIT は上位ベンチマークの 3 体）。知覚は視野角、見通し判定、ゲーム内の音イベントだけに基づき、ウォールハックや全知情報は使いません。物資収集、回復、移動、漁夫行動、高度な移動も行います。
- **完全なルート経済** — フロアルート、3 段階の宝箱（標準／エリート／保管庫）、医療キット、シールドセル、5 つの共通インベントリスロット、弾薬プール。
- **プライベートオンラインルーム** — ボットなしの人間 2〜4 人、最大 4 人枠、10 人までのボット補充、チーム戦、人間対ボット、フレンドリーファイアなし、戦術ピング、ゲストの 60 秒再接続。切断中のゲストはニュートラル入力となり、ボットによる乗っ取りはありません。
- **磨き込まれた表現** — PBR マテリアル、マップごとの動的ライティング、ブルーム、弾道・マズルフラッシュ・着弾・破片、流血を伴わないスタイライズされた撃破エフェクト、空間化された CC0 効果音、環境音、独自のメニュー／リザルト音。

## 操作方法（初期設定、変更可能）

| アクション | キー |
| --- | --- |
| 移動 | `W A S D` |
| 視点／射撃／ADS | マウス／左クリック／右クリック |
| ジャンプ／ダブルジャンプ | `Space` |
| スプリント | `L-Shift` |
| しゃがみトグル／スライディング | `L-Ctrl` |
| 近接攻撃 | `Q` |
| ダッシュ | `R-Shift` |
| グラップル | `F` |
| グラウンドパウンド | `C` |
| インタラクト | `E` |
| リロード | `R` |
| インベントリスロット | `1–5` |
| 武器を捨てる | `X` |
| 医療キット／シールドセル | `G`／`H` |
| 一人称／三人称カメラ | `V` |
| 全体マップ情報／観戦切り替え | `M`／`←` `→` |

## ブラウザ基準

デスクトップ版 Chrome、Edge、Firefox、Safari を対応対象とします。リリース証跡はブラウザとネットワークごとに分けて扱います。自動オンラインハーネスは分離した Chromium コンテキストを使い、Firefox と Safari は別途ブラウザ QA が必要です。キーボードとマウスを備えたデスクトップブラウザが必要で、タッチ端末には未対応入力であることを明示します。

## 開発

```bash
npm ci
npm run dev          # Vite 開発サーバー: http://localhost:5173
```

```bash
npm run build        # 型検査 + dist/ への本番ビルド
npm run preview      # 本番ビルドをローカル配信
npm run test         # 単体 + 統合テスト（Vitest）
npm run sim -- map=neocity difficulty=hard count=3   # ヘッドレスのボットマッチ
npm run audit:assets                 # 本番アセット集合 + SHA-256
npm run audit:secrets                # 追跡対象／無視対象外ソースのスキャン
npm run audit:licenses               # 依存ライセンス方針の検査
npm run audit:zero-cost              # STUN 専用コスト不変条件 + レシート
npm run cloudflare:dry-run           # ビルド + Wrangler 検証（アップロードなし）
npx tsx tests/browser/qa-maps.ts     # 任意のローカルブラウザ QA（Playwright）
```

アーキテクチャの概要は [開発ガイド](docs/DEVELOPMENT.md)、テスト方針の詳細は [テスト戦略](docs/TESTING.md) を参照してください。

## Cloudflare へのデプロイ

本番ビルドは既存の [Workers Static Assets](https://developers.cloudflare.com/workers/static-assets/) 設定から静的バンドルとして配信します。これはファイル配信のみで、マルチプレイヤー権限を持つ Worker ルートではありません。

```bash
npm run build
npx wrangler deploy      # wrangler.jsonc を使用
```

詳細は [デプロイガイド](docs/DEPLOYMENT.md) を参照してください。

## アーキテクチャ概要

固定タイムステップのマッチシミュレーション（`src/sim`）を、描画（`src/render`）、音声（`src/audio`）、UI（`src/ui`）、入力（`src/player`）から完全に分離したシミュレーション優先設計です。人間とボット（`src/ai`）のコントローラーはいずれも `InputCommand` を生成するため、ボットはプレイヤーとまったく同じルールで動きます。物理演算は Rapier（WASM）、ナビゲーションはジャンプ／よじ登り／落下／水泳リンクを持つ生成済みの多層ナビグラフ、コンテンツはマップ定義からデータ駆動で構築されます。詳細は [アーキテクチャ](docs/ARCHITECTURE.md)、[ボット AI](docs/BOT_AI.md)、[ゲームデザイン](docs/GAME_DESIGN.md) を参照してください。

## リポジトリ構成

```text
src/               ゲーム、シミュレーション、描画、音声、UI のソース
tests/             単体、統合、シミュレーション、任意のブラウザ検査
public/            本番専用の静的ファイルと再配布アセット
scripts/           再現可能なリポジトリ／リリース監査
docs/              アーキテクチャ、設計、テスト、デプロイ、来歴
.github/            CI、コントリビューション用テンプレート、依存更新
```

## ライセンスとアセット

コードは [MIT License](LICENSE) で公開します。表現層は、独自にコード生成されるシステム（VFX、水面、ストーム、UI、キャラクター衣装／組み立て）と、CC0／パブリックドメインの第三者アート・音声パックを組み合わせています。来歴、アセット単位のライセンス、取得日、SHA-256 チェックサムは [docs/ASSET_MANIFEST.md](docs/ASSET_MANIFEST.md)、[docs/ASSET_CHECKSUMS.txt](docs/ASSET_CHECKSUMS.txt)、[THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md) に記録しています。実行時 CDN からの取得はなく、すべてのアセットをビルドへ同梱します。

## コントリビューション

Issue と PR を歓迎します。詳細は [CONTRIBUTING.md](CONTRIBUTING.md) を参照してください。セキュリティ報告は [SECURITY.md](SECURITY.md)、コミュニティ参加は [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md) に従います。
